<!DOCTYPE html>
<html>
  <head>
    <title>DesafioDev</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <main class='container mt-4'>
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= flash_class(key) %> alert-dismissible fade show" role="alert">
          <%= value %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>
    <footer class='d-flex flex-wrap bg-dark text-white justify-content-between align-items-center py-4 border-top'>
      <div class="col-md-4 d-flex align-items-center">
        <a href="/" class="mb-3 me-2 mb-md-0 text-body-secondary text-decoration-none lh-1">
          <svg class="bi" width="30" height="24"><use xlink:href="#bootstrap"></use></svg>
        </a>
        <span class="mb-3 mb-md-0 text-body-secondary">
          © 2023 ByCoders, <a href='https://github.com/geraldoantonio/desafio-dev' target='_blank'>Geraldo Júnior</a>
        </span>
      </div>
    </footer>
  </body>
</html>
